"""SQLAlchemy models.

Placeholder for Task 2 (database setup): `app.db.init_db()` imports this
module so all model classes register on `Base.metadata` before
`create_all()` runs. Task 3 (data models) replaces this file with the real
model definitions (User, DailySummary, etc).
"""
